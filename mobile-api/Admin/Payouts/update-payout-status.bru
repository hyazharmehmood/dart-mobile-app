meta {
  name: update-payout-status
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/api/admin/payouts/{{payoutId}}
  body: json
  auth: none
}

headers {
  Cookie: {{adminSessionCookie}}
}

body:json {
  {
    "status": "APPROVED"
  }
}
