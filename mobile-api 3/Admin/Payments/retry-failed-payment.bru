meta {
  name: retry-failed-payment
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/api/admin/payments/failed/{{transactionId}}
  body: none
  auth: none
}

headers {
  Cookie: {{adminSessionCookie}}
}
