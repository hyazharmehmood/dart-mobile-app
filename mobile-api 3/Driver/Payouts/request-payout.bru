meta {
  name: request-payout
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/driver/payouts
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{driverAccessToken}}
}

body:json {
  {
    "amount": 50,
    "payoutMethodId": "{{payoutMethodId}}",
    "runDate": "2026-06-15"
  }
}
